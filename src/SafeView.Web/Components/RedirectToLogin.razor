@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        var uri = Uri.EscapeDataString(Nav.ToBaseRelativePath(Nav.Uri));
        Nav.NavigateTo($"/login?returnUrl=/{uri}", forceLoad: true);
    }
}
